info:
  build:
    name: Authorization Service
    description: Unified Authorization Trap for all ReportPortal's Services
    version: '@version@'
    branch: '@branch@'

spring:
  application:
    name: uat

security:
  sessions: stateless
  basic:
    enabled: false
