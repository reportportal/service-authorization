info:
  name: Authorization Service
  description: Authorization Service
  build:
    version: '@version@'
    branch: '@branch@'

spring:
  application:
    name: uat

security:
  sessions: stateless
  basic:
    enabled: false
